---
description: TrotBot 文档动态更新与追踪规范（需求、架构、问题、README）
alwaysApply: true
---

# TrotBot 文档动态更新规则

适用目标：开发与联调过程中，保持需求、架构、问题记录、README 与代码一致。

## 1) 三份核心文档更新触发条件

- 修改/新增功能、参数、话题、消息、launch、状态机后：同步更新 `docs/trotbot/软件需求清单.md`（完成状态、代码依据、未闭环项）。
- 变更系统分层、节点职责、数据流、启动编排后：同步更新 `docs/trotbot/架构说明.md`（只写当前事实，不写过期方案）。
- 任何开发/联调/实测异常、答疑结论、临时绕过、根因、回归结果：同步更新 `docs/trotbot/开发问题跟踪.md`（状态流转 Open/In Progress/Resolved/Closed）。

## 2) README 动态更新规则

- 任何目录内新增可被他人直接使用的能力（命令、脚本、配置、接口）时，更新该目录 `README.md`。
- 至少覆盖：用途、依赖、启动命令、关键参数、验证方法、常见故障与排查。
- 若改动影响根级使用方式（构建、启动、运行路径），同步更新仓库根 `README.md`。

## 3) 更新粒度与质量要求

- 文档内容必须可执行、可验证：给出真实命令、参数名、话题名、文件路径。
- 需求文档只维护“需求与实现状态”，不混入排期；架构文档只维护“当前实现”。
- 问题跟踪每条记录必须包含：现象、复现、期望/实际、影响、绕过、根因、修复、验证、结论。

## 4) 代码与文档一致性检查（每次交付前）

- 检查参数名与默认值是否与代码声明一致（`declare_parameter` 与 YAML 一致）。
- 检查 launch 参数文件来源是否明确（避免 src 与 install 配置不一致）。
- 检查 README 命令是否可直接运行，路径与包名是否真实存在。

## 5) 优先级与冲突处理

- 若代码事实与文档冲突：以代码运行事实为准，立即修正文档并记录原因。
- 若用户口径与文档冲突：先记录到问题跟踪，再按用户确认结果更新需求与架构文档。
